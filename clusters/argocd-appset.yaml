apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: applications
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - &git_generator
      git:
        repoURL: https://github.com/seanturner026/argocd-applicationset.git
        revision: wip
        directories:
          - path: apps/infra/argo-rollouts
        values:
          namespace: argo-rollouts

    # # - <<: *git_generator
    - git:
        repoURL: https://github.com/seanturner026/argocd-applicationset.git
        revision: wip
        directories:
          - path: apps/infra/nginx
        values:
          namespace: nginx

  template:
    metadata:
      name: "{{.path.basename}}"
      namespace: "{{.values.namespace}}"
    spec:
      project: default
      source:
        repoURL: https://github.com/seanturner026/argocd-applicationset.git # Can this be gotemplated?
        targetRevision: wip # ditto
        path: "{{.path.path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.path.basename}}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
          selfHeal: true
